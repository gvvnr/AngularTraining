import { CompressDataPipe } from './compress-data.pipe';

describe('CompressDataPipe', () => {
  it('create an instance', () => {
    const pipe = new CompressDataPipe();
    expect(pipe).toBeTruthy();
  });
});
